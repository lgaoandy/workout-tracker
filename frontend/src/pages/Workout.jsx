import { useEffect } from "react"
import AreaButton from "../components/AreaButton/AreaButton"

export default function Workout() {
    useEffect(() => {}, [])

    return (
        <div>
            <AreaButton>Add Exercise</AreaButton>
        </div>
    )
}
